import React from 'react';
import { ProtectedRouteInterfaceProps } from './interface';

export const ProtectedRoute: React.FC<ProtectedRouteInterfaceProps> =
  (props) => {

    return (
      <div>

      </div>
    );
  };
